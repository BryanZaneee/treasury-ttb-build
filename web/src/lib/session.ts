/** Mock session — PRD §8 has no user accounts. The single definition, so the
 *  name shown and the name recorded cannot disagree. */
export const REVIEWER = {
  name: 'J. Park',
  role: 'Compliance Agent',
  initials: 'JP',
} as const
